'use client';

import { DesignerPage } from '@/presentation/pages/DesignerPage';

/**
 * Home Page
 *
 * Renders the main Eraser DesignerPage component.
 * This page is embeddable via iframe for integration
 * into other applications.
 */
export default function Home() {
  return <DesignerPage />;
}
